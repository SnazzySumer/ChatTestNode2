var mongoose = require('mongoose');

var Schema = mongoose.Schema;

var userSchema = mongoose.Schema({ 
	name : String,
	mobno: String, 
	reg_id: String
});

mongoose.connect('localhost:27017/nodejsserver');
module.exports = mongoose.model('users', userSchema);        
